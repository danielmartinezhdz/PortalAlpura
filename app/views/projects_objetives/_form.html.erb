<%= form_for(projects_objetive) do |f| %>
  <% if projects_objetive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(projects_objetive.errors.count, "error") %> prohibited this projects_objetive from being saved:</h2>

      <ul>
      <% projects_objetive.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :project_id %>
    <%= f.text_field :project_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
